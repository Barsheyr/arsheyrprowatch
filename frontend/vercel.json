{
  "version": 2,
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    {
      "src": "/categories/(.*)",
      "dest": "/categories/[slug]"
    },
    {
      "src": "/watches/(.*)",
      "dest": "/watches/[slug]"
    }
  ]
}
